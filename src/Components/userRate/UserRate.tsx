import React, { useEffect, useRef, useState } from 'react'
import UserStars from './UserStars'

const UserRate = () => {

    


    // const form = useRef(null)
    // useEffect(() => {

    //     form.current.addEventListener('click' , (e) => {
    //         e.preventDefault()
    //     })
    // },[])

  return (
    <div>
        
    </div>
  )
}

export default UserRate